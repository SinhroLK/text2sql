# Copy this file to .env and fill values only when a real provider is added.
GROQ_API_KEY=

# Optional demo database settings. Phase 0 uses a local SQLite fixture instead.
DB_HOST=127.0.0.1
DB_PORT=3306
DB_USER=text2sql_readonly
DB_PASSWORD=
DB_NAME=

