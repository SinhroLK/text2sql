# Demo application

The Gradio application will be rebuilt after the validator and sandbox executor are available. The original `app.py` is preserved as legacy material and is not used because it executes generated SQL without the planned safety controls.

