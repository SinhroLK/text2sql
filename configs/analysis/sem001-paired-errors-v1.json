{
  "schema_version": 1,
  "analysis_id": "sem001-paired-semantic-errors-v1",
  "scope": "development",
  "arm_order": ["B1", "B6R", "B4", "B5"],
  "target_arm": "B5",
  "categories": [
    "output_shape",
    "table_or_column",
    "join_path_or_cardinality",
    "filter_or_literal",
    "aggregation_or_grouping",
    "ordering_limit_or_ties",
    "date_time_or_window",
    "recursion_or_set_operation",
    "sqlite_dialect_or_syntax"
  ],
  "expected_example_ids": [
    "local009",
    "local010",
    "local068",
    "local070",
    "local071",
    "local072",
    "local167",
    "local168",
    "local169",
    "local170",
    "local171",
    "local202",
    "local244",
    "local269",
    "local270",
    "local272",
    "local273",
    "local274",
    "local275",
    "local277",
    "local279",
    "local286",
    "local309",
    "local310",
    "local311",
    "local335",
    "local336",
    "local344",
    "local354",
    "local355",
    "local356"
  ],
  "arms": {
    "B1": {
      "baseline": "B1",
      "predictions": {
        "path": "artifacts/experiments/exp001-b1-predictions.jsonl",
        "sha256": "b85afc0bf08bc513eb639cad2e2fc05e151d6b7a9f2907c2746e7dfd163ea7b9"
      },
      "report": {
        "path": "artifacts/reports/exp001-b1-report.json",
        "sha256": "296f9725977faa88bd53236221d32402d1c0c849611c36c31405d313230ac375"
      }
    },
    "B6R": {
      "baseline": "B6R",
      "predictions": {
        "path": "artifacts/experiments/exp004-b6r-predictions.jsonl",
        "sha256": "46664f819ce0d8faa2fe377babda3a6555df38477e0570415df146345df26577"
      },
      "report": {
        "path": "artifacts/reports/exp004-b6r-report.json",
        "sha256": "aaa130ec8e2f606058c413ac9434f49b9af618ec2fb5da45214f4896bd7802fc"
      }
    },
    "B4": {
      "baseline": "B4",
      "predictions": {
        "path": "artifacts/experiments/exp006-b4-predictions.jsonl",
        "sha256": "d64cd4f5e74cecce79c4aa465b8c762246b25241b5ec2e9f7b850b8f3c7bc580"
      },
      "report": {
        "path": "artifacts/reports/exp006-b4-report.json",
        "sha256": "0d2e995ebc7ce2e63b45b8416f301d84794cfe10ece35ee3a0739bccb3c5307a"
      }
    },
    "B5": {
      "baseline": "B5",
      "predictions": {
        "path": "artifacts/experiments/dspy001-b5-predictions.jsonl",
        "sha256": "47fd965671ad8ae6a65c9e007649c4065c68803d10ce9f9e0cb114020ae697e7"
      },
      "report": {
        "path": "artifacts/reports/dspy001-b5-report.json",
        "sha256": "878be5ed4fb0c6b96c2195ef16cf4bd19350c0a308eb58dc29aa91c408499f9e"
      }
    }
  },
  "labels": {
    "local009": {
      "primary": "table_or_column",
      "secondary": ["aggregation_or_grouping"],
      "rationale": "Uses aircraft range as route distance instead of deriving the distance between the departure and destination airports."
    },
    "local010": {
      "primary": "aggregation_or_grouping",
      "secondary": ["ordering_limit_or_ties"],
      "rationale": "Returns a hard-coded zero and omits city-pair averaging, distance bucketing, and selection of the least-populated bucket."
    },
    "local068": {
      "primary": "date_time_or_window",
      "secondary": ["sqlite_dialect_or_syntax"],
      "rationale": "Nests SUM as an argument to LAG in the same SELECT level, which SQLite rejects as window-function misuse."
    },
    "local070": {
      "primary": "date_time_or_window",
      "secondary": ["output_shape"],
      "rationale": "Builds streaks over duplicate city rows rather than distinct dates, so row numbers split the wrong islands and do not guarantee one representative city per date."
    },
    "local071": {
      "primary": "filter_or_literal",
      "secondary": ["date_time_or_window"],
      "rationale": "Never restricts the source rows to June 2022, so the longest-streak calculation is performed over the complete date history."
    },
    "local072": {
      "primary": "output_shape",
      "secondary": ["date_time_or_window"],
      "rationale": "Returns only country, run length, and a ratio while omitting the evaluated distinct-day, streak-boundary, row-count, and capital-count fields."
    },
    "local167": {
      "primary": "join_path_or_cardinality",
      "secondary": ["date_time_or_window"],
      "rationale": "Groups every represented state rather than anchoring each legislator to the state of the first term, and its broad date predicates do not test service on an actual December 31."
    },
    "local168": {
      "primary": "join_path_or_cardinality",
      "secondary": ["aggregation_or_grouping"],
      "rationale": "The direct skill join repeats a salary once for every matching top-three skill, producing a skill-weighted average instead of one salary contribution per qualifying posting."
    },
    "local169": {
      "primary": "output_shape",
      "secondary": ["date_time_or_window"],
      "rationale": "Omits total-cohort and retained-legislator counts and reports a fraction rather than the evaluated percentage; retention is also based only on the first term row."
    },
    "local170": {
      "primary": "table_or_column",
      "secondary": ["date_time_or_window"],
      "rationale": "References nonexistent legislators_terms.gender instead of joining the legislator gender source, so the query cannot execute."
    },
    "local171": {
      "primary": "date_time_or_window",
      "secondary": ["join_path_or_cardinality"],
      "rationale": "Computes elapsed years from each active term rather than from each legislator's first term and does not explicitly evaluate service on December 31."
    },
    "local244": {
      "primary": "aggregation_or_grouping",
      "secondary": ["output_shape"],
      "rationale": "Adds genre to the grouping and output, yielding 60 genre-length rows instead of the three requested duration categories."
    },
    "local269": {
      "primary": "recursion_or_set_operation",
      "secondary": ["join_path_or_cardinality"],
      "rationale": "Seeds every packaging relation as a root, so nested containers are treated as final top-level combinations when leaf quantities are averaged."
    },
    "local270": {
      "primary": "recursion_or_set_operation",
      "secondary": ["aggregation_or_grouping"],
      "rationale": "Carries only the edge quantity during recursion rather than multiplying quantities across hierarchy levels, leaving every accumulated item total below the intended threshold."
    },
    "local272": {
      "primary": "join_path_or_cardinality",
      "secondary": ["aggregation_or_grouping"],
      "rationale": "Joins each picking line to every inventory row for the product instead of its allocated location and never performs the required cumulative FIFO allocation."
    },
    "local273": {
      "primary": "aggregation_or_grouping",
      "secondary": ["output_shape"],
      "rationale": "Averages picked-line quantity divided by order-line quantity rather than computing FIFO overlap ranges before aggregation, and its product output does not match the evaluated identifier shape."
    },
    "local277": {
      "primary": "aggregation_or_grouping",
      "secondary": ["date_time_or_window"],
      "rationale": "Computes a simple historical average and omits the required seasonality adjustment, time-step window, weighted regression, and 2018 annual forecast."
    },
    "local279": {
      "primary": "recursion_or_set_operation",
      "secondary": ["table_or_column"],
      "rationale": "Initializes recursion from only the single inventory row with the global maximum ID, so most products never enter the monthly inventory simulation."
    },
    "local286": {
      "primary": "output_shape",
      "secondary": ["join_path_or_cardinality"],
      "rationale": "Omits total quantity sold, reports packing duration in days instead of hours, and uses joins/aggregates that admit two extra seller rows."
    },
    "local309": {
      "primary": "output_shape",
      "secondary": ["ordering_limit_or_ties"],
      "rationale": "Returns driver and constructor point totals in addition to the requested year, driver name, and constructor name, so the 75-row result has the wrong projection."
    },
    "local310": {
      "primary": "table_or_column",
      "secondary": ["aggregation_or_grouping"],
      "rationale": "The outer query references r.year although only the agg subquery exists in that scope, causing an execution error before yearly maxima can be compared."
    },
    "local311": {
      "primary": "aggregation_or_grouping",
      "secondary": ["output_shape"],
      "rationale": "Combines MAX driver-standing points with a sum of repeated constructor-standing rows instead of deriving each year's best driver and team totals, and emits constructor IDs rather than names."
    },
    "local335": {
      "primary": "aggregation_or_grouping",
      "secondary": ["join_path_or_cardinality"],
      "rationale": "Finds constructors with the lowest constructor-wide points, while the question first requires the lowest point-scoring drivers per season and then their constructors."
    },
    "local336": {
      "primary": "output_shape",
      "secondary": ["aggregation_or_grouping"],
      "rationale": "Returns one hard-coded four-column zero row instead of deriving one category/count row for each of the four overtake causes."
    },
    "local344": {
      "primary": "join_path_or_cardinality",
      "secondary": ["date_time_or_window"],
      "rationale": "Joins every lap position to every pit stop in the race, multiplying events, and does not correctly establish the previous-lap ahead/behind pair before classifying the change."
    },
    "local355": {
      "primary": "filter_or_literal",
      "secondary": ["join_path_or_cardinality"],
      "rationale": "Filters short participation spans rather than gaps of fewer than three missed races and omits the required constructor switch across each hiatus."
    },
    "local356": {
      "primary": "filter_or_literal",
      "secondary": ["join_path_or_cardinality"],
      "rationale": "Excludes current-lap pit stops only; pit exits on the following lap and retirement-related changes remain, so non-track position movements dominate the count."
    }
  }
}
